import banner from "../../../assets/hero-banner.webp"
const Hero = () => {
    return (
        <div style={{backgroundImage:`url(${banner})`}} className="h-[80vh] bg-cover bg-center bg-no-repeat">

        </div>
    );
};

export default Hero;